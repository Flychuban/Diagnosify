import React from "react"
export default Feed: React.FC = () => {
    return (
        <>
            {diagnoses.map((diagnose) => {
                return (    

                )
            })}
        </>
    )
}
